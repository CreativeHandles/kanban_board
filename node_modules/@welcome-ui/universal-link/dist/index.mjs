"use client"

// src/index.tsx
import React from "react";
import { forwardRef } from "@welcome-ui/system";

// src/styles.ts
import styled from "@xstyled/styled-components";
import { shouldForwardProp } from "@welcome-ui/system";
var UniversalLink = styled.aBox.withConfig({ shouldForwardProp })`
  color: inherit;
  text-decoration: none;
`;

// src/index.tsx
var UniversalLink2 = forwardRef(
  ({ children, dataTestId, target, ...rest }, ref) => /* @__PURE__ */ React.createElement(
    UniversalLink,
    {
      color: "inherit",
      "data-testid": dataTestId,
      ref,
      rel: target === "_blank" ? "noopener noreferrer" : void 0,
      target,
      ...rest
    },
    children
  )
);
UniversalLink2.displayName = "UniversalLink";
export {
  UniversalLink2 as UniversalLink
};
