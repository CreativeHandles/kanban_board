import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeVariantIcon = {
    danger: CSSObject;
    default: CSSObject;
    info: CSSObject;
    success: CSSObject;
    warning: CSSObject;
};
export declare const getVariantIcon: (theme: WuiTheme) => ThemeVariantIcon;
