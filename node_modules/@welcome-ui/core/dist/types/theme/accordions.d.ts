import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeAccordions = {
    content: CSSObject;
    icon: CSSObject;
    padding: string;
    title: CSSObject;
    wrapper: CSSObject;
};
export declare const getAccordions: (theme: WuiTheme) => ThemeAccordions;
