import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemePopovers = {
    content: CSSObject;
    default: CSSObject;
    title: CSSObject;
};
export declare const getPopovers: (theme: WuiTheme) => ThemePopovers;
