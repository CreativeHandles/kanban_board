import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
type Sizes = 'xs' | 'sm' | 'md' | 'lg' | 'auto';
export type ThemeModals = {
    backdrop: CSSObject;
    body: CSSObject;
    cover: Record<string, unknown>;
    default: CSSObject;
    footer: CSSObject;
    gutter: string;
    header: CSSObject;
    sizes: Record<Sizes, {
        width?: string;
    }>;
};
export declare const getModals: (theme: WuiTheme) => ThemeModals;
export {};
