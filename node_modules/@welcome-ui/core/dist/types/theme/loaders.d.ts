import { WuiTheme } from './types';
export type ThemeLoaders = {
    lg: string;
    md: string;
    sm: string;
    xs: string;
};
export declare const getLoaders: ({ toRem }: WuiTheme) => ThemeLoaders;
