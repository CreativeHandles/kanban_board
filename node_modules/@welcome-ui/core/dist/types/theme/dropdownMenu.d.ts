import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeDropdownMenu = {
    inner: CSSObject;
    item: CSSObject;
    separator: CSSObject;
};
export declare const getDropdownMenu: (theme: WuiTheme) => ThemeDropdownMenu;
