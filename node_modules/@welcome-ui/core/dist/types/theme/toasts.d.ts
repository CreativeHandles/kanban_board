import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeToasts = {
    bottom: CSSObject;
    default: CSSObject;
    growls: {
        default: CSSObject;
        title: CSSObject;
    };
    snackbar: {
        default: CSSObject;
        separator: {
            danger: CSSObject;
            default: CSSObject;
            info: CSSObject;
            success: CSSObject;
            warning: CSSObject;
        };
    };
    top: CSSObject;
};
export declare const getToasts: (theme: WuiTheme) => ThemeToasts;
