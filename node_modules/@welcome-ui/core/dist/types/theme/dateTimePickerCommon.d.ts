import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeDateTimePickerCommon = {
    item: {
        selected: CSSObject;
        today: CSSObject;
    };
};
export declare const getDateTimePickerCommon: (theme: WuiTheme) => ThemeDateTimePickerCommon;
