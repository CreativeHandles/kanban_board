import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeCheckboxes = {
    checked: CSSObject;
    default: CSSObject;
    disabled: CSSObject;
};
export declare const getCheckboxes: (theme: WuiTheme) => ThemeCheckboxes;
