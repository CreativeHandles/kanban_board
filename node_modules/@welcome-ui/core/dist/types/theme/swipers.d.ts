import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeSwipers = {
    navigation: {
        bullet: {
            active: CSSObject;
            default: CSSObject;
        };
    };
};
export declare const getSwipers: (theme: WuiTheme) => ThemeSwipers;
