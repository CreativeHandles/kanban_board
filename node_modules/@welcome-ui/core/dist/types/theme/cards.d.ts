import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeCards = {
    default: CSSObject;
};
export declare const getCards: (theme: WuiTheme) => ThemeCards;
