import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeSliders = {
    default: CSSObject;
    disabled: CSSObject;
    focused: CSSObject;
    output: CSSObject;
    rangeInput: CSSObject;
    selector: CSSObject;
};
export declare const getSliders: (theme: WuiTheme) => ThemeSliders;
