import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeAccordions = {
    default: CSSObject;
    disabled: {
        default: CSSObject;
        primary: CSSObject;
    };
    sizes: {
        md: CSSObject;
        sm: CSSObject;
    };
    variants: {
        default: CSSObject;
        primary: CSSObject;
    };
};
export declare const getBadges: (theme: WuiTheme) => ThemeAccordions;
