import { CSSObject } from '@xstyled/styled-components';
import { WuiTheme } from './types';
export type ThemeHints = {
    danger: CSSObject;
    default: CSSObject;
    success: CSSObject;
    warning: CSSObject;
};
export declare const getHints: (theme: WuiTheme) => ThemeHints;
